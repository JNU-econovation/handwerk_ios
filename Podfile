# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'handwerk_second' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'


  # Pods for handwerk_second

  target 'handwerk_secondTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'handwerk_secondUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
